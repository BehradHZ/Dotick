# v0.1.0 — Increment 0 foundation record

Prepared and verified: 2026-09-09.

Status: **verified Increment 0 foundation record**. Increment 0 is formally closed. This record does not claim that a public Git tag, GitHub Release, or production deployment has been published.

Implementation commit: `88aa5986a5673224a3d4ea78fca164f742e9d83e`

Hosted verification: GitHub Actions run `34330581198` — successful.

## Included

- Reproducible Django/PostgreSQL API and Expo web workspaces with locked Python and npm dependency graphs.
- UUID user baseline, Argon2 password hashing, and case-insensitive email uniqueness.
- Developer-only authenticated Foundation Checkpoint create/list/read flow with owner isolation and hardened API boundaries.
- Responsive Walking Skeleton web client with in-memory developer credentials, sign-out cleanup, checkpoint create/list/refresh, loading/error/retry handling, and draft preservation.
- Executable UTC/timezone/DST/all-day Golden Time Vectors plus Gregorian/Jalali calendar conversion tests.
- Vitest/Testing Library client tests and Playwright desktop/mobile persistence E2E tests.
- Non-root API/web containers and PostgreSQL + API + web Compose topology.
- Hosted CI covering static checks, clean database migrations, tests, web export, E2E, audits, secret scanning, deployment checks, container builds, smoke tests, and persistence across API restart.

## Verification summary

The successful hosted run performed a clean repository checkout and installed Python dependencies with `uv sync --frozen` and frontend dependencies with `npm ci`.

Results:

- 30 Django backend tests passed.
- 14 backend Golden Time tests passed.
- 8 client component/network/calendar tests passed.
- 2 Playwright Walking Skeleton tests passed across desktop and mobile viewports.
- Fresh PostgreSQL migration verification passed on a separately created empty database.
- Ruff, ESLint, Prettier, TypeScript, traceability, schema-drift checks, and Expo web export passed.
- Django `check --deploy --fail-level WARNING` passed with the Foundation workbench disabled.
- Python dependency audit reported no known vulnerabilities.
- npm high-severity audit gate passed; 10 moderate transitive Expo/native-tooling findings remain accepted and documented technical debt.
- Gitleaks reported no committed secret leaks.
- API and web images built successfully and both runtime containers executed as non-root users.
- Container smoke verification passed, including retrieval of a persisted Checkpoint after restarting the API container.

## Foundation boundary

Checkpoints are disposable engineering verification data. They do not define Task/Event/Routine behavior or later history/sync semantics. Developer HTTP Basic authentication is isolated to the local/test Foundation workbench and is replaced by product authentication in its owning increment. Credentials are not persisted by the client.

Golden Time cases owned by later increments for recurrence and Dotick-Day attribution remain acceptance handoffs rather than false Increment 0 implementation claims.

## Accepted technical debt

- Temporary developer-only HTTP Basic transport.
- Disposable Foundation Checkpoint resource.
- Ten moderate transitive npm findings in Expo/native tooling; no high/critical blockers.
- Walking Skeleton scope is web verification rather than native/PWA product distribution.
- No public tag/release/deployment is implied by this engineering release record.

## Closure

The Increment 0 Foundation gate set is complete and has green hosted evidence. The project may proceed to Increment 1 using this foundation baseline.
